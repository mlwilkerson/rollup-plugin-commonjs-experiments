const alpha = {num: 1, name: 'one'}
const beta = {num: 2, name: 'two'}
const gamma = {num: 3, name: 'three'}
const elements = {
  alpha,
  beta,
  gamma
}
export default elements
export { alpha, beta, gamma }
